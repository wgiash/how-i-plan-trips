# Montauk Trip

Separate one-page trip app in the same format as the Hudson Valley roadtrip (the app at the repo root). Not linked from it; deploys as its own Vercel project from this directory.

```sh
npm install
npm run dev
```
